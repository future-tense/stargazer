{
  "name": "stargazer-wallet",
  "version": "1.5.2",
  "description": "ionic: An Ionic project",
  "dependencies": {
    "gulp": "^3.5.6",
    "gulp-sass": "^2.0.4",
    "gulp-concat": "^2.2.0",
    "gulp-minify-css": "^0.3.0",
    "gulp-rename": "^1.2.0"
  },
  "devDependencies": {
    "bower": "^1.3.3",
    "gulp-util": "^2.2.14",
    "shelljs": "^0.3.0"
  },
  "scripts": {
    "start": "npm run clean && npm run copy-app && ionic run android",
    "copy-app": "cp -R ../dist/* www",
    "clean": "rm -rf www/*"
  },
  "cordovaPlugins": [
    "https://github.com/VersoSolutions/CordovaClipboard.git",
    "cordova-plugin-device",
    "cordova-plugin-inappbrowser",
    "cordova-plugin-statusbar",
    "cordova-plugin-whitelist",
    "ionic-plugin-keyboard",
    "phonegap-plugin-barcodescanner"
  ],
  "cordovaPlatforms": [
    "android"
  ]
}
